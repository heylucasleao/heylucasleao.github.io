---
{"dg-publish":true,"permalink":"/digital-garden/probabilistic-forecasting/text/4-previsao-pontual/"}
---


# 4. Previsão pontual

Há um grande desafio em conseguir diferenciar o sinal do ruído dentro da previsão, como descrevi anteriormente. Esses métodos e as formas como podem ser utilizados para prever variam de modelo para modelo. Não irei detalhar muito os tipos mais comuns, mas passarei brevemente pelos univariados e multivariados e abordarei dois métodos menos comuns de previsão do *target* "y".

<br>

## Métodos univariados e multivariados

Os métodos mais antigos são conhecidos como **univariados** e estatísticos e analisam o sinal – como o _Autoregressive Integrated Moving Average_ (ARIMA) e o _Error, Trend, and Seasonality_ (ETS) – de forma endógena. Isto é, apenas o comportamento passado da própria variável preditora auxilia na previsão futura; como exemplo, o preço de ontem da gasolina auxilia a prever o preço de amanhã. Entretanto, nos dias de hoje, apenas isso não demonstra o cenário inteiro.

Modelos **multivariados**, que podem englobar tanto múltiplos sinais diferentes da mesma variável – como _lag 7_ e _lag 1_ – quanto variáveis que se correlacionam com ela – como, por exemplo, o preço do petróleo ou a inflação – conseguem detectar essas flutuações de forma mais sensível. Não existe o certo ou o errado: vai depender da disponibilidade de variáveis e do quão previsível e complexo é o seu dado.

<br>

## Modelos híbridos

Há também outra opção mista entre esses métodos. Com base na ideia de [MSTL](https://nixtlaverse.nixtla.io/statsforecast/docs/models/multipleseasonaltrend.html), decompomos o sinal em tendência, sazonalidade e resíduo (ruído):

- Utilizamos o MSTL para observar a integridade dos sinais;

- Para a **tendência**, utilizamos algum modelo autorregressivo, pois, em teoria, ela pode ser explicada por si mesma (ETS, por exemplo, estima e processa bem nestes cenários);

- A **sazonalidade** será salva para ser repetida nos dados futuros (a biblioteca Nixtla já possui uma classe simples para auxiliar nisso. Podemos repetir os valores por ciclo ou utilizar novamente uma média móvel exponencial – um ETS – para tal.);

- Isso nos deixa com o **ruído**. Como ele não pode ser explicado por movimentos naturais da variável que queremos prever, podemos utilizar um modelo de _Machine Learning_, que conseguirá atuar de forma não linear. Caso a extração da tendência e da sazonalidade tenha funcionado bem, o ruído ficará com bastante informação não linear e quase nenhuma informação autorregressiva. Por isso, um modelo de ML funcionaria bem, pois poderia captar os sinais das variáveis exógenas sobre o ruído que queremos mapear de forma mais "limpa".

Por fim, somamos os resultados da tendência, da sazonalidade e do ruído previsto. Isso gerará a previsão do movimento futuro. Deixarei [exemplos](https://github.com/heylucasleao/tinyshift/blob/main/tinyshift/examples/dmstl.ipynb) nos quais fiz dessa forma.

Essa abordagem não garante sucesso, mas nos permite preservar a interpretabilidade. Como teremos três sinais, teremos a demonstração da tendência natural de nossa variável e de sua sazonalidade, além de podermos aplicar o SHAP isoladamente no modelo de _Machine Learning_ para entender os comportamentos não lineares, facilitando a estimativa, por exemplo, de taxas de crescimento ou períodos de alta demanda. Sua desvantagem, em contrapartida, decorre justamente da decomposição: em cenários nos quais trabalhamos com milhares de SKUs, isso pode se tornar impraticável, pois cada SKU terá que ter seus modelos estatísticos. Ainda assim, caso sejam modelos de suavização exponencial, não há tanto problema nisso, embora ainda exista um problema de orquestração. Temos a possibilidade de atuar com um modelo local ou global exclusivamente para os resíduos.

<br>

### DTL & DMSTL

No `tinyshift`, podemos modelar essa abordagem de duas formas utilizando um sistema de `Wrapper`, no qual uma classe orquestradora irá receber outra, com a ideia de envolvê-la da mesma forma que um `wraps` funciona em um *decorator* – ou na vida real.

<br>

#### DTL: tendência e resíduo

O `DTLWrapper` é voltado a séries sem sazonalidade relevante. Para cada `SKU`, ele:

1. Extrai uma tendência robusta por LOWESS;
2. Prevê essa tendência com um modelo do `StatsForecast`, por exemplo, `AutoETS`;
3. Calcula o resíduo, isto é, a parcela que a tendência não explica;
4. Modela o resíduo com `MLForecast`;
5. Soma tendência prevista e resíduo previsto para obter a previsão final.

O LOWESS é robusto a observações pontuais extremas e permite que a tendência seja uma curva, e não apenas uma reta. Isso é útil quando a demanda possui uma direção estrutural lenta, mas os desvios em torno dela dependem de lags, preço, promoções ou outras variáveis exógenas.

```text
Série por SKU
  ├── LOWESS → tendência → StatsForecast
  └── resíduo → lags/PAMI → MLForecast local ou global
                         │
                         ▼
          tendência futura + resíduo futuro
                         │
                         ▼
                  previsão final
```

<br>

#### DMSTL: tendência, sazonalidade e resíduo

O `DMSTLWrapper` é a extensão para séries com uma ou mais sazonalidades. Ele executa MSTL por série e separa tendência, componentes sazonais e resíduo. A tendência e cada sazonalidade são previstas por modelos estatísticos (ou apenas pela repetição dos valores sazonais); a parcela residual fica com o modelo de ML. No fim, o *wrapper* recompõe as três previsões.

Quando `season_length="auto"`, os períodos sazonais candidatos são detectados por FFT, individualmente para cada série. Quando `nlags="auto"`, PAMI escolhe a defasagem residual a partir da dependência entre padrões ordinais presentes e passados. Isso evita impor, por exemplo, a mesma sazonalidade semanal ou o mesmo conjunto de lags a todos os SKUs.

```text
Série por SKU
       │
       ▼
      MSTL
  ├── tendência → StatsForecast
  ├── sazonalidades → StatsForecast
  └── resíduo → lags/PAMI → MLForecast local ou global
                              │
                              ▼
       tendência futura + sazonalidades futuras + resíduo futuro
                              │
                              ▼
                       previsão final
```

<br>

#### Local ou global?

Em ambos os *wrappers*, tendência e sazonalidade continuam específicas de cada série. A escolha local/global se aplica ao modelo de resíduos:

- **Local:** um modelo residual por SKU. Faz sentido quando cada série possui dinâmica própria e histórico suficiente.

- **Global:** um único modelo residual treinado no painel inteiro. Faz sentido quando há muitas séries relacionadas, pouco histórico por SKU ou ganho de escala por compartilhar padrões entre elas.

No modo global, cada série ainda pode ter períodos sazonais e lags escolhidos individualmente; o modelo residual recebe a união ordenada dos lags selecionados. Assim, o compartilhamento global ocorre onde pode ajudar a aprender relações residuais, sem apagar a estrutura temporal local de cada SKU. Em contrapartida, a modelagem com milhares de SKUs sobre dados intermitentes, por exemplo, pode ser prejudicial.

<br>

## Previsão hierárquica (_Hierarchical Forecasting_)

Outra abordagem é o uso de forecasting hierárquico. Nesse contexto, prevemos a demanda agregada em diferentes níveis de granularidade (ex.: País $\rightarrow$ Região $\rightarrow$ Categoria $\rightarrow$ SKU), o que garante estabilidade nos níveis mais altos e precisão operacional nos níveis mais baixos, mantendo a coerência matemática entre todas as pontas da organização.

As abordagens mais comuns para conciliação hierárquica são:

- **Top-Down:** A previsão é gerada primariamente no nível mais agregado (ex.: demanda total da empresa) e, em seguida, desagregada para os níveis inferiores com base em proporções históricas. É útil para reduzir o ruído e a volatilidade das séries de baixo nível, embora possa mascarar tendências específicas de SKUs individuais.

- **Bottom-Up:** A previsão é gerada no nível mais granular (ex.: SKU por loja) e somada para compor os níveis superiores. Captura nuances específicas de cada ponto de venda ou produto, mas tende a acumular ruído e maior variabilidade nas agregações superiores.

- **Middle-Out:** Combina os dois mundos ao realizar a previsão em um nível intermediário (ex.: por categoria ou região). A partir dele, o modelo desagrega os valores para os níveis inferiores (_bottom_) e agrega para os níveis superiores (_top_).

- **Métodos de Conciliação Ótima (ex.: MinT / _Minimum Trace_):** Em vez de apenas agregar ou desagregar de forma rígida, geram-se previsões independentes para todos os níveis da hierarquia e utiliza-se uma matriz de covariância dos erros para reconciliar as séries, garantindo a coerência (a soma das partes é exatamente igual ao todo) e minimizando a variância do erro final.

> **Dica de Implementação**
> Bibliotecas modernas de ecossistemas como a [**Nixtla**](https://nixtlaverse.nixtla.io/hierarchicalforecast/index.html) (com o framework `HierarchicalForecast`) possuem rotinas eficientes e otimizadas para aplicar esses métodos de reconciliação de forma escalável.

<br>

>**Nota:** Algo interessante de se pontuar é que tanto a metodologia de modelo híbrido quanto a de modelo hierárquico podem ser utilizadas em outras abordagens, inclusive em conjunto. Só lembre que, conforme a complexidade aumentar, deverá haver uma justificativa para isso. Não há vergonha em ter modelos *baseline* simples rodando em um primeiro momento antes de, por exemplo, fazer a decomposição via DMSTL, aplicar um *stacking* de dez modelos de gradiente com diferentes configurações e inserir uma camada hierárquica no final. Obviamente, essa é uma visão extrema, mas serve apenas para dar uma ideia.
